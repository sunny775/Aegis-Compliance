import { createTheme } from '@mui/material/styles';

/**
 * Placeholder MUI theme. Palette, typography, and component overrides are
 * defined in a later phase; for now this is an empty (default) theme so the
 * ThemeProvider has something to render with.
 */
export const theme = createTheme({});
